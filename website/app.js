/* AI-Nexus Radar — Flexible Scraper Studio */
(() => {
  'use strict';

  const $ = (id) => document.getElementById(id);
  const sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms));
  const MAX_LOCAL_ROWS = 5000;
  const MAX_RENDER_ROWS = 10000;
  const COLLECTOR_WAIT_ATTEMPTS = 420; // ~17.5 minutes at 2.5s/poll
  const SCRAPE_WAIT_ATTEMPTS = 240; // ~10 minutes at 2.5s/poll

  const state = { fields: [['title','Primary title or name'],['url','Canonical URL'],['description','Main description or summary'],['price','Price or compensation when present']], rows: [], columns: [], collectorId: '', lastGoodRows: [], lastRun: null, mode: 'run', bindingComplete: false };
  const esc = (value) => String(value ?? '').replace(/[&<>"']/g, (char) => ({ '&':'&amp;', '<':'&lt;', '>':'&gt;', '"':'&quot;', "'":'&#39;' })[char]);

  function log(message, replace = false) {
    const element = $('log'); if (!element) return;
    const time = new Date().toLocaleTimeString([], { hour12:false });
    element.textContent = replace ? `[${time}] ${message}` : `${element.textContent}\n[${time}] ${message}`;
    element.scrollTop = element.scrollHeight;
  }
  function toast(message) { const element=$('toast'); if(!element)return; element.textContent=message;element.classList.add('show');window.setTimeout(()=>element.classList.remove('show'),2600); }
  function setStatus(text, live=false) { if($('statusText'))$('statusText').textContent=text;if($('statusDot'))$('statusDot').className=live?'live':''; }
  function getUrls() { return ($('urls')?.value||'').split(/\r?\n|,/).map(v=>v.trim()).filter(Boolean); }
  function getSchemaDescription() { return state.fields.map(([key,description])=>({key:key.trim(),description:description.trim()})).filter(x=>x.key).map(x=>`${x.key} — ${x.description}`).join('; '); }
  function getCollectionDescription() { const base=$('description')?.value.trim()||getSchemaDescription(); return `${base}\n\nPagination policy: follow normal next-page/pagination controls until the source is exhausted. Do not impose an artificial page limit.`; }
  function hasExplicitCollector() { return Boolean(($('collectorId')?.value||state.collectorId).trim()); }
  function wantsNewCollector() { if(hasExplicitCollector()) return false; const reuse=$('reuseCollector'); if(reuse)return !reuse.checked; return Boolean($('autoCreate')?.checked); }

  function normalizeRows(payload) {
    if(Array.isArray(payload))return payload;
    if(!payload||typeof payload!=='object')return [];
    const candidates=[payload.data,payload.results,payload.items,payload.rows,payload.records,payload.dataset,payload.result,payload.data?.data,payload.data?.results,payload.data?.items,payload.data?.rows,payload.data?.records];
    for(const candidate of candidates)if(Array.isArray(candidate))return candidate;
    const lifecycleOnly=['status','state','pending','message','error','error_code','code'];
    if(Object.keys(payload).every(k=>lifecycleOnly.includes(k)))return [];
    return [payload];
  }

  function renderFields() {
    const container=$('fields');if(!container)return;
    container.innerHTML=state.fields.map(([key,description],index)=>`<div class="fieldChip"><span class="drag">⋮⋮</span><input class="fieldKey" data-index="${index}" value="${esc(key)}"><input class="fieldDesc" data-index="${index}" value="${esc(description)}"><button class="removeField" data-index="${index}" type="button" aria-label="Remove field">×</button></div>`).join('');
    container.querySelectorAll('.fieldKey,.fieldDesc').forEach(input=>input.addEventListener('input',()=>{const index=Number(input.dataset.index);if(!Number.isInteger(index)||!state.fields[index])return;state.fields[index][input.classList.contains('fieldKey')?0:1]=input.value;}));
    container.querySelectorAll('.removeField').forEach(button=>button.addEventListener('click',()=>{const index=Number(button.dataset.index);if(!Number.isInteger(index))return;state.fields.splice(index,1);renderFields();log(`Schema field ${index+1} removed.`);}));
  }

  function renderRows(rows) {
    state.rows=Array.isArray(rows)?rows:[];
    state.columns=[...new Set(state.rows.flatMap(row=>Object.keys(row||{})))];
    if(!state.columns.length)state.columns=state.fields.map(([key])=>key.trim()).filter(Boolean);
    const table=$('resultTable');
    if(table){const head=table.querySelector('thead'),body=table.querySelector('tbody');if(head)head.innerHTML=`<tr>${state.columns.map(c=>`<th>${esc(c)}</th>`).join('')}</tr>`;if(body){const visible=state.rows.slice(0,MAX_RENDER_ROWS);body.innerHTML=visible.length?visible.map(row=>`<tr>${state.columns.map(column=>`<td>${esc(Array.isArray(row?.[column])?row[column].join(', '):row?.[column]??'')}</td>`).join('')}</tr>`).join(''):'<tr><td class="empty">Run a scraper to populate results.</td></tr>';if(state.rows.length>MAX_RENDER_ROWS)body.insertAdjacentHTML('afterbegin',`<tr><td colspan="${Math.max(state.columns.length,1)}" class="empty">Showing the first ${MAX_RENDER_ROWS.toLocaleString()} rows. The complete dataset remains available through export.</td></tr>`);}}
    if($('jsonView'))$('jsonView').textContent=JSON.stringify(state.rows,null,2);
    if($('resultMeta')){const timestamp=state.lastRun?.collectedAt?new Date(state.lastRun.collectedAt).toLocaleString():'not run';$('resultMeta').textContent=`${state.rows.length.toLocaleString()} rows · ${state.columns.length} fields · ${timestamp}`;}
    try{localStorage.setItem('nexusRadarRows',JSON.stringify(state.rows.slice(0,MAX_LOCAL_ROWS)));localStorage.setItem('nexusRadarMeta',`${state.rows.length} rows · ${state.columns.length} fields`);localStorage.setItem('nexusRadarTruncated',String(state.rows.length>MAX_LOCAL_ROWS));}catch(error){log(`Local result cache skipped: ${error.message}`);}
  }

  function auditRows(rows=state.rows){const required=state.fields.map(([key])=>key.trim()).filter(Boolean),data=Array.isArray(rows)?rows:[];const checks=required.map(key=>{let missing=0;for(const row of data){const value=row?.[key],empty=value===null||value===undefined||(typeof value==='string'&&!value.trim())||(Array.isArray(value)&&value.length===0);if(empty)missing++;}const ratio=data.length?missing/data.length:1;return{key,missing,ratio,ok:data.length>0&&state.columns.includes(key)&&ratio<.3};});const coverage=checks.length?checks.filter(c=>c.ok).length/checks.length:0;const completeness=checks.length?checks.reduce((s,c)=>s+(1-c.ratio),0)/checks.length:data.length?1:0;return{score:data.length?Math.round((coverage*.5+completeness*.5)*100):0,checks};}
  function updateTimeline(activeStep){document.querySelectorAll('.timelineStep').forEach((el,i)=>{el.classList.toggle('active',i<=activeStep);el.classList.toggle('done',i<activeStep);});}
  function renderHealth(result,label='Audit complete'){const{score,checks}=result,drift=checks.some(c=>!c.ok);if($('healthScore'))$('healthScore').textContent=score;if($('healthBig'))$('healthBig').textContent=score;if($('rowMetric'))$('rowMetric').textContent=state.rows.length.toLocaleString();if($('fieldMetric'))$('fieldMetric').textContent=state.columns.length;if($('driftMetric'))$('driftMetric').textContent=checks.filter(c=>!c.ok).length;if($('healthSummary'))$('healthSummary').textContent=score>=85?'Healthy extraction. Schema and completeness look stable.':score>=60?'Extraction needs attention. Review flagged fields.':'Extraction is unhealthy. Trigger self-heal before trusting downstream data.';if($('fieldHealth'))$('fieldHealth').innerHTML=checks.length?checks.map(c=>`<div class="fieldHealthRow"><span><b>${esc(c.key)}</b><small>${c.missing}/${state.rows.length} empty · ${Math.round(c.ratio*100)}% missing</small></span><strong class="${c.ok?'ok':'bad'}">${c.ok?'HEALTHY':'DRIFT'}</strong></div>`).join(''):'<div class="empty">No requested fields.</div>';if($('reliabilityState')){$('reliabilityState').textContent=drift?'DRIFT DETECTED':'HEALTHY';$('reliabilityState').className=`badge ${drift?'bad':'ok'}`;}if($('reliabilityRun'))$('reliabilityRun').textContent=label;updateTimeline(drift?1:0);return result;}

  async function api(path,options={}){
    let response;
    try{response=await fetch(path,{cache:'no-store',...options,headers:{'Content-Type':'application/json',...(options.headers||{})}});}catch(error){throw new Error(`Cannot reach ${path}. Check the Vercel deployment/API route. ${error.message||'Network request failed.'}`);}
    let body={};try{body=await response.json();}catch{}
    if(!response.ok){const detail=body?.error||body?.message||`Request failed (HTTP ${response.status})`;throw new Error(typeof detail==='string'?detail:JSON.stringify(detail));}
    return body;
  }

  async function loadConfiguredCollector(silent=false){try{const result=await api('/api/collector');if(result.collectorId){state.collectorId=result.collectorId;if($('collectorId')&&!$('collectorId').value.trim())$('collectorId').value=result.collectorId;if($('collectorHint'))$('collectorHint').textContent=`Configured collector available: ${result.collectorId}`;if(!silent)log(`Configured collector available: ${result.collectorId}`);return result.collectorId;}if($('collectorHint'))$('collectorHint').textContent='No default collector configured. New collector mode is ready.';}catch(error){if(!silent)log(`Collector discovery: ${error.message}`);if($('collectorHint'))$('collectorHint').textContent='Collector discovery unavailable. You can still create a new collector.';}return '';}

  async function waitCollector(id){for(let attempt=1;attempt<=COLLECTOR_WAIT_ATTEMPTS;attempt++){const progress=await api(`/api/collector?collectorId=${encodeURIComponent(id)}`),status=String(progress.status||progress.state||'').toLowerCase();if(['done','completed','ready','success','available'].includes(status))return progress;if(['failed','error','cancelled','canceled'].includes(status))throw new Error(`Collector generation ${status}.`);if(attempt===1||attempt%8===0)log(`Collector generation: ${progress.status||'running'} (${attempt}/${COLLECTOR_WAIT_ATTEMPTS})`);await sleep(2500);}throw new Error('Collector generation exceeded the 17.5-minute UI wait window. The collector may still be generating; check Bright Data Scraper Studio and reuse the collector ID once ready.');}

  async function createCollector(url,description){log('Creating a new Bright Data collector…');const created=await api('/api/collector',{method:'POST',body:JSON.stringify({url,description:getCollectionDescription(),name:`nexus-${Date.now()}`})});if(!created.collectorId)throw new Error('Bright Data did not return a collector ID.');state.collectorId=created.collectorId;if($('collectorId'))$('collectorId').value=created.collectorId;log(`Collector created: ${created.collectorId}`);await waitCollector(created.collectorId);log('Collector is available.');return created.collectorId;}

  async function pollResult(runToken){if(!runToken)throw new Error('Bright Data did not return a run identifier.');for(let attempt=1;attempt<=SCRAPE_WAIT_ATTEMPTS;attempt++){const result=await api(`/api/run?responseId=${encodeURIComponent(runToken)}`),status=String(result.status||result.state||'').toLowerCase();if(status==='done'||status==='completed'||status==='success'){const data=normalizeRows(result.data);log(`Scrape complete · ${data.length.toLocaleString()} rows received.`);if(!data.length){const diagnostic=result.diagnostics?` Response keys: ${(result.diagnostics.responseKeys||[]).join(', ')||'(none)'}. Upstream status: ${result.diagnostics.upstreamStatus||'(none)'}.`:'';throw new Error(`Bright Data completed but returned no records.${diagnostic}`);}return data;}if(status==='empty')throw new Error(result.message||'Bright Data completed the job but returned no records.');if(status==='missing')throw new Error(result.error||'Bright Data no longer has this scrape result.');if(['failed','error','cancelled','canceled'].includes(status))throw new Error(`${result.error||`Scrape job ${status}.`}${result.error_code?` [${result.error_code}]`:''}`);if(attempt===1||attempt%8===0)log(`Scrape pending… (${attempt}/${SCRAPE_WAIT_ATTEMPTS})`);await sleep(2500);}throw new Error(`Scrape exceeded the 10-minute UI wait window. Response ID: ${runToken}. You can retry the result from Bright Data if it is still running.`);}

  async function runOne(collectorId,url){log(`Starting scrape: ${url}`);const started=await api('/api/run',{method:'POST',body:JSON.stringify({collectorId,url})});const runToken=started.responseId;if(!runToken)throw new Error('Bright Data returned no run identifier.');log('Bright Data accepted the scrape job.');return pollResult(runToken);}

  async function runScraper(){const targets=getUrls(),description=$('description')?.value.trim()||getSchemaDescription();let collectorId=$('collectorId')?.value.trim()||state.collectorId;const explicitCollector=Boolean(collectorId);if(!targets.length)return toast('Add at least one public URL.');if(!description)return toast('Describe what you want to extract.');const button=$('runBtn');if(button)button.disabled=true;setStatus('RUNNING',true);log('Starting flexible scraper…',true);try{if(state.mode==='create'){if(explicitCollector)throw new Error('An existing collector ID is entered. Switch to Run mode to reuse it, or clear the ID to create a new collector.');collectorId=await createCollector(targets[0],description);setStatus('READY',true);toast(`Collector ${collectorId} created.`);return;}if(!explicitCollector)collectorId=await loadConfiguredCollector(true)|| (wantsNewCollector()?await createCollector(targets[0],description):'');if(!collectorId)throw new Error('No collector is available. Enter an existing collector ID or enable Auto-create new collector.');state.collectorId=collectorId;if($('collectorId'))$('collectorId').value=collectorId;const allRows=[];for(const target of targets)allRows.push(...await runOne(collectorId,target));state.lastGoodRows=JSON.parse(JSON.stringify(allRows));state.lastRun={collectedAt:new Date().toISOString()};renderRows(allRows);const health=renderHealth(auditRows(allRows),'Live dataset audited');log(`DONE · ${allRows.length.toLocaleString()} rows · health ${health.score}/100`);setStatus(health.score>=60?'HEALTHY':'DRIFT',health.score>=60);if(health.checks.some(c=>!c.ok)&&$('healPrompt'))$('healPrompt').value=`Repair collector ${collectorId}. Fields with extraction gaps: ${health.checks.filter(c=>!c.ok).map(c=>c.key).join(', ')}. Preserve schema: ${getSchemaDescription()}.`;$('results')?.scrollIntoView({behavior:'smooth',block:'start'});}catch(error){log(`ERROR · ${error.message}`);setStatus('ERROR');toast(error.message);}finally{if(button)button.disabled=false;}}

  function auditReliability(){if(!state.rows.length)return toast('Run a scraper or simulate field drift first.');const result=renderHealth(auditRows(state.rows),'Reliability audit complete');log(`Reliability audit complete · ${state.rows.length.toLocaleString()} rows · ${result.checks.filter(c=>!c.ok).length} drifted fields`);$('health')?.scrollIntoView({behavior:'smooth',block:'start'});}
  function simulateDrift(){if(!state.rows.length){state.rows=[{title:'Demo record A',url:'https://example.com/a',description:'Healthy demo row',price:'$120'},{title:'Demo record B',url:'https://example.com/b',description:'Healthy demo row',price:'$180'},{title:'Demo record C',url:'https://example.com/c',description:'Healthy demo row',price:'$240'}];state.columns=['title','url','description','price'];state.lastGoodRows=JSON.parse(JSON.stringify(state.rows));}const target=state.fields.map(([key])=>key.trim()).find(key=>key&&state.columns.includes(key))||state.columns[0];state.rows=state.rows.map((row,index)=>({...row,[target]:index===0?row[target]:null}));renderRows(state.rows);const result=renderHealth(auditRows(state.rows),'Simulated extraction drift');const check=result.checks.find(item=>item.key===target);if($('healState')){$('healState').textContent='DRIFT DETECTED';$('healState').className='badge bad';}if($('healPrompt')){const collector=state.collectorId||$('collectorId')?.value.trim()||'(current collector)';$('healPrompt').value=`Field “${target}” is empty in ${check?.missing||0}/${state.rows.length} rows. Repair collector ${collector} while preserving schema: ${getSchemaDescription()}.`;}log(`SIMULATED DRIFT · ${target}`);updateTimeline(1);$('heal')?.scrollIntoView({behavior:'smooth',block:'start'});}
  function restoreClean(){if(!state.lastGoodRows.length)return toast('No clean baseline exists yet.');state.rows=JSON.parse(JSON.stringify(state.lastGoodRows));renderRows(state.rows);renderHealth(auditRows(state.rows),'Restored clean baseline');if($('healState')){$('healState').textContent='WAITING';$('healState').className='badge';}log('Restored clean dataset.');}
  async function heal(){const collectorId=$('collectorId')?.value.trim()||state.collectorId,prompt=$('healPrompt')?.value.trim();if(!collectorId)return toast('No collector selected.');if(!prompt)return toast('Describe what broke.');$('healBtn').disabled=true;$('approveBtn').disabled=true;setStatus('HEALING',true);if($('healState')){$('healState').textContent='HEALING';$('healState').className='badge warn';}updateTimeline(2);log(`Triggering self-heal for ${collectorId}…`);try{const started=await api('/api/heal',{method:'POST',body:JSON.stringify({collectorId,prompt})});if($('healPreview'))$('healPreview').textContent=JSON.stringify(started,null,2);const status=String(started.status||'RUNNING').toUpperCase();if($('healState')){$('healState').textContent=status;$('healState').className='badge warn';}$('approveBtn').disabled=false;log(`SELF-HEAL ${status} · review the repair before approval.`);}catch(error){if($('healState')){$('healState').textContent='ERROR';$('healState').className='badge bad';}log(`HEAL ERROR · ${error.message}`);toast(error.message);}finally{$('healBtn').disabled=false;}}
  async function approve(){const collectorId=$('collectorId')?.value.trim()||state.collectorId;if(!collectorId)return toast('No collector selected.');$('approveBtn').disabled=true;try{const result=await api('/api/heal',{method:'PUT',body:JSON.stringify({collectorId})});if($('healPreview'))$('healPreview').textContent=JSON.stringify(result,null,2);if($('healState')){$('healState').textContent=result.status||'APPROVED';$('healState').className='badge ok';}updateTimeline(3);log('Repair approved. Re-run the collector to verify recovery.');}catch(error){$('approveBtn').disabled=false;toast(error.message);log(`APPROVE ERROR · ${error.message}`);}}
  function download(name,text,type){const url=URL.createObjectURL(new Blob([text],{type})),link=document.createElement('a');link.href=url;link.download=name;document.body.appendChild(link);link.click();link.remove();window.setTimeout(()=>URL.revokeObjectURL(url),500);}
  function csv(){if(!state.rows.length)return toast('No data to export.');const escapeCsv=value=>`"${String(value??'').replaceAll('"','""')}"`;const lines=[state.columns.map(escapeCsv).join(','),...state.rows.map(row=>state.columns.map(column=>{const raw=row?.[column];return escapeCsv(Array.isArray(raw)?raw.join('; '):raw??'');}).join(','))];download('nexus-radar.csv',lines.join('\n'),'text/csv');}
  function bind(){if(state.bindingComplete)return;state.bindingComplete=true;$('runBtn')?.addEventListener('click',runScraper);$('auditBtn')?.addEventListener('click',auditReliability);$('driftBtn')?.addEventListener('click',simulateDrift);$('restoreBtn')?.addEventListener('click',restoreClean);$('healBtn')?.addEventListener('click',heal);$('approveBtn')?.addEventListener('click',approve);$('addField')?.addEventListener('click',()=>{const raw=$('newField')?.value.trim();if(!raw)return toast('Enter a field name.');const[key,...description]=raw.split('|').map(v=>v.trim());if(!key)return toast('Enter a field name.');state.fields.push([key,description.join(' | ')||'Requested extraction field']);$('newField').value='';renderFields();log(`Added schema field: ${key}`);});$('exampleAmazon')?.addEventListener('click',()=>{$('urls').value='https://books.toscrape.com/';$('description').value='Extract one row per book with title, price, availability, rating, category, and product URL. Follow all available pagination until exhausted.';log('Loaded e-commerce example with full-pagination collection.');});$('exampleDocs')?.addEventListener('click',()=>{$('urls').value='https://example.com/docs';$('description').value='Extract document title, section heading, summary, author and canonical URL. Follow all available pagination until exhausted.';log('Loaded documentation example with full-pagination collection.');});$('clearUrls')?.addEventListener('click',()=>{$('urls').value='';log('Target URLs cleared.');});$('collectorId')?.addEventListener('input',()=>{const hasId=Boolean($('collectorId').value.trim());if(hasId){if($('reuseCollector'))$('reuseCollector').checked=true;if($('autoCreate'))$('autoCreate').checked=false;if($('collectorHint'))$('collectorHint').textContent='Existing collector ID detected. Reuse mode is active.';}});$('reuseCollector')?.addEventListener('change',()=>{const reuse=$('reuseCollector').checked;if($('collectorHint'))$('collectorHint').textContent=reuse?'Reuse mode active. Enter or keep the existing collector ID.':'New collector mode is active. A fresh Bright Data collector will be created.';if(reuse&&!$('collectorId')?.value.trim())loadConfiguredCollector();});$('autoCreate')?.addEventListener('change',()=>{if($('collectorHint'))$('collectorHint').textContent=$('autoCreate').checked?'New collector mode is active. A fresh Bright Data collector will be created.':'Reuse mode active. Enter an existing collector ID.';});$('viewTable')?.addEventListener('click',()=>{$('tableWrap')?.classList.remove('hidden');$('jsonView')?.classList.add('hidden');$('viewTable')?.classList.add('active');$('viewJson')?.classList.remove('active');});$('viewJson')?.addEventListener('click',()=>{$('tableWrap')?.classList.add('hidden');$('jsonView')?.classList.remove('hidden');$('viewJson')?.classList.add('active');$('viewTable')?.classList.remove('active');});$('downloadCsv')?.addEventListener('click',csv);$('downloadJson')?.addEventListener('click',()=>{if(!state.rows.length)return toast('No data to export.');download('nexus-radar.json',JSON.stringify(state.rows,null,2),'application/json');});$('copyLog')?.addEventListener('click',async()=>{try{await navigator.clipboard.writeText($('log')?.textContent||'');toast('Activity copied.');}catch{toast('Clipboard access unavailable.');}});document.querySelectorAll('.navlinks a[href^="#"]').forEach(link=>link.addEventListener('click',event=>{const target=document.querySelector(link.getAttribute('href'));if(target){event.preventDefault();target.scrollIntoView({behavior:'smooth',block:'start'});}}));}
  function init(){bind();renderFields();renderRows([]);renderHealth(auditRows([]),'Waiting for a scrape');if($('approveBtn'))$('approveBtn').disabled=true;loadConfiguredCollector(true);log('All workspace controls initialized.');}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();
